# VERA platform state — end of 2026-08-03
One-page truth for the next agent (any lane). Verified, not assumed.

## What VERA is now
A verification-first NYC rental platform: a curated daily drop (max 8
full-fit listings), Steward Grades (A–E from cited city records, '?'
never inflates), ownership chain-of-proof, computed scam tells (relist,
contact reuse, description clones, photo clones by perceptual hash,
AI-photo classifier read), price memory no relist can reset, value-vs-
hood-median math, commute anchors quoting the MTA timetable, receipts
archive, print field kits, corrections channel, fair-housing statement,
PWA. Suite: **91 in-page tests, green** (run `?test=1`).

## Surfaces
- App: littlefightnyc.com/vera (website repo `/app/public/vera/`, main)
- Feed: vera-pipeline.netlify.app/data/public.json (+ archive.json)
- Static: /vera/manual/, /vera/archive/, /vera/corrections/
- Emailer: full-fit alerts to David within a minute of publish (proven 2×)

## Automation
- Mac launchd: nightly 06:00 UTC full sweep+publish (residential-IP sources)
- GitHub Actions `sanctioned-cloud-sweep`: daily 05:30 UTC — official CSVs,
  GTFS, HF AI-photo pass; `cloud-discovery` job runs the FULL pipeline with
  `VERA_CLOUD=1` (skips renthop/leasebreak/housing_connect — datacenter-
  blocked; everything else probed 200 from runners incl. craigslist sapi)
- Cloud publish + email-ingestion + Reddit OAuth wait ONLY on David's
  credentials (configs/*.example stubs; 2 gh secrets) — code all shipped

## Approved decisions on record
Forensic weights applied (−10/−15/−25/−20 cap −45, David 2026-08-03);
Option A hybrid active; Kimi's hq fallback VERA = digest only.

## Hard-won rules (cost real hours)
- `build_snapshot`'s STATE_ROOT is cache/state; durable stores live in
  top-level state/. Never conflate.
- The website checkout is SHARED across three agents — `git branch
  --show-current` before every push; VERA work goes on main.
- Vite dev serves cached index.html — bust the DOCUMENT URL when testing.
- The nightly ET-date guard is consumed even by failed runs
  (state/schedule_guards/nightly.json → clear `last_et_date` to rerun).
- Feed contract: configs/snapshot_schema.md is current through today.

## The line that governs everything
Every number is computed from a cited record or marked ≈; no data = "?",
never an A. No pan/zoom map, no AI chat, no urgency theater, no contact
with landlords, ever.
